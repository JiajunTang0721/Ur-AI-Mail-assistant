# Local Data Templates

这个目录放的是后端运行时会读取和写入的本地 JSON 文件。

- `preferences.json`: 默认偏好模板
- `feedback.json`: 空反馈模板

当前仓库中的版本已经做了脱敏处理，只保留了适合公开仓库展示的默认内容。

如果你在本地联调后产生了真实账号数据、反馈记录或测试结果，建议在提交到 GitHub 前重新清空或替换成模板内容。
